language: cpp

compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get update -qq

install:
  - sudo apt-get install -qq cmake 'libxcb.*-dev'

before_script:
  - mkdir build
  - cd build

script:
  - cmake ..
  - make
  - ctest --output-on-failure

env:
  - CXXFLAGS=
  - CXXFLAGS=-std=c++0x
